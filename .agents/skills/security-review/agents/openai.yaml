interface:
  display_name: "Security Review"
  short_description: "Audit security boundaries and critical guarantees"
  default_prompt: "Use $security-review to audit security boundaries and critical guarantees within the requested scope."
